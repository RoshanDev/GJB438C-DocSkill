# 验证范围（0.4.0）

验证以当前提交的 CI 日志为准，不复用旧提交上的 PASS。测试源码直接位于 `skills/gjb438c-md-first/tests/`；工作流不改写或提交运行时。

- 参数回归：分隔/等号形式、重复 profile 参数均通过同一解析器，最终 release 不能走草稿路径。
- 发布集：内容报告、格式报告、体量报告和 Word 一起暂存；Word 最后替换；replace 或随后 fsync 失败回滚。多文件替换不是全局原子快照，突然掉电需检查锁和备份。
- 20 类无根模板初始化、字段/条目门禁、草稿 Word 往返、单独 wheel 安装后独立运行。
- 套件审计入口直接导入 `audit_suite_manifest`；仅选中且本轮通过审计的文档可满足基线依赖。
- 真实 LibreOffice 目录刷新、正文起点、Word 内容绑定和修改后的候选回流。
- 合成 300 页仅检验分页与重复检测，不代表项目内容通过。旧的两条需求示例不能作为大型项目 release 样例。

自动检查不证明来源材料真实、不认证批准人的身份，也不替代逐页视觉评审。正式交付需要项目所有者确认实际审批、字体环境与各项内容证据。
